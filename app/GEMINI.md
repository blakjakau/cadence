# Gemini CLI Context
npm start is never needed, a separate external process automatically handless this

avoid effusive and potentially patronizing language, e.g. "excellent point", "amazing observation", etc. acknowledge, but tone it back a little.

only consider files in ./js and ./css unless explicitly instructed

don't attempt to run test or lint commands
don't offer to commit changes